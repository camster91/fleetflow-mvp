import {clearDriverAssignments} from '../../lib/teamDriverCleanup'
test('atomically clear helper unassigns both snapshots only in the exact team',async()=>{const tx:any={vehicle:{updateMany:jest.fn().mockResolvedValue({count:2})},delivery:{updateMany:jest.fn().mockResolvedValue({count:3})}};await expect(clearDriverAssignments(tx,'team-a','driver-a')).resolves.toEqual({vehicles:2,deliveries:3});for(const model of [tx.vehicle,tx.delivery])expect(model.updateMany).toHaveBeenCalledWith({where:{teamId:'team-a',assignedDriverId:'driver-a'},data:{assignedDriverId:null,driver:null}})})
