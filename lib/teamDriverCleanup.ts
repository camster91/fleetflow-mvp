import type { Prisma } from '@prisma/client'
export async function clearDriverAssignments(tx:Prisma.TransactionClient,teamId:string,userId:string|null|undefined,audit?:{actorId:string;actorName?:string|null;actorRole?:string|null}){if(!userId)return {vehicles:0,deliveries:0};const [vehicles,deliveries]=await Promise.all([tx.vehicle.updateMany({where:{teamId,assignedDriverId:userId},data:{assignedDriverId:null,driver:null}}),tx.delivery.updateMany({where:{teamId,assignedDriverId:userId},data:{assignedDriverId:null,driver:null}})]);if(audit&&(vehicles.count||deliveries.count))await tx.auditLog.create({data:{userId:audit.actorId,teamId,userName:audit.actorName??null,userRole:audit.actorRole??null,action:'updated',entityType:'driver_assignment',entityId:userId,description:`Cleared ${vehicles.count} vehicle and ${deliveries.count} delivery assignments after driver access changed`}});return {vehicles:vehicles.count,deliveries:deliveries.count}}
